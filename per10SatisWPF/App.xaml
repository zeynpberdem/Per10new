<Application x:Class="per10SatisWPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="LoginWindow.xaml">
    <Application.Resources>

        <!-- Renkler -->
        <SolidColorBrush x:Key="BgDark"      Color="#1E2130"/>
        <SolidColorBrush x:Key="BgCard"      Color="#252A3A"/>
        <SolidColorBrush x:Key="BgPanel"     Color="#1A1E2D"/>
        <SolidColorBrush x:Key="AccentBlue"  Color="#3A7BF7"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#2ECC71"/>
        <SolidColorBrush x:Key="AccentRed"   Color="#E74C3C"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#F39C12"/>
        <SolidColorBrush x:Key="TextWhite"   Color="#ECEFF4"/>
        <SolidColorBrush x:Key="TextGray"    Color="#8892A4"/>
        <SolidColorBrush x:Key="BorderColor" Color="#2D3348"/>
        <SolidColorBrush x:Key="NumpadBg"    Color="#2A2F42"/>

        <!-- Yuvarlak köşeli buton şablonu -->
        <ControlTemplate x:Key="RoundBtnTemplate" TargetType="Button">
            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Opacity" Value="0.88"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="border" Property="Opacity" Value="0.75"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Küçük yuvarlak buton (adet +/-) -->
        <ControlTemplate x:Key="SmallRoundBtnTemplate" TargetType="Button">
            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Numpad buton stili -->
        <Style x:Key="NumpadStyle" TargetType="Button">
            <Setter Property="Background"       Value="{StaticResource NumpadBg}"/>
            <Setter Property="Foreground"       Value="{StaticResource TextWhite}"/>
            <Setter Property="FontSize"         Value="18"/>
            <Setter Property="FontWeight"       Value="Bold"/>
            <Setter Property="BorderThickness"  Value="0"/>
            <Setter Property="Margin"           Value="2"/>
            <Setter Property="Cursor"           Value="Hand"/>
            <Setter Property="Template"         Value="{StaticResource RoundBtnTemplate}"/>
        </Style>

    </Application.Resources>
</Application>
